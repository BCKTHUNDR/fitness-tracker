[project]
name = "fitness-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "BCKTHUNDR", email = "07150k1337@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pytest>=9.1.1",
]

[project.scripts]
fitness-tracker = "fitness_tracker:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
